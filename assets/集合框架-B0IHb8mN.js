import{i as e,r as t,s as n,t as r}from"./app-BqBvIiBt.js";var i=JSON.parse(`{"path":"/one-stage/java/notes/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6.html","title":"Java 集合底层原理剖析","lang":"zh-CN","frontmatter":{},"git":{"contributors":[{"name":"lwl-root","username":"lwl-root","email":"80236507+lwl-root@users.noreply.github.com","commits":1,"url":"https://github.com/lwl-root"}],"changelog":[{"hash":"065b4fca7a7ae8d65cdbc7ac125c3403d9df103c","time":1782461798000,"email":"80236507+lwl-root@users.noreply.github.com","author":"卢文龙","message":"Update deploy.yml"}]},"filePathRelative":"one-stage/java/notes/集合框架.md"}`),a={name:`集合框架.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="java-集合底层原理剖析" tabindex="-1"><a class="header-anchor" href="#java-集合底层原理剖析"><span>Java 集合底层原理剖析</span></a></h1><h2 id="_1-java-集合介绍" tabindex="-1"><a class="header-anchor" href="#_1-java-集合介绍"><span>1. Java 集合介绍</span></a></h2><p>Java 集合是一个存储相同类型数据的容器，类似数组，集合可以不指定长度，但是数组必须指定长度。集合类主要从 <strong>Collection</strong> 和 <strong>Map</strong> 两个根接口派生出来，比如常用的 ArrayList、LinkedList、HashMap、HashSet、ConcurrentHashMap 等等。</p><ul><li><p>Collection 根接口框架简化结构图（线程不安全）：</p><img src="/javabook-v2/images/collection-1.png"></li><li><p>Collection 根接口框架简化结构图（线程安全）：</p><img src="/javabook-v2/images/collection-2.png"></li><li><p>Map 根接口框架简化结构图（线程不安全）：</p><img src="/javabook-v2/images/map-1.png"></li><li><p>Map 根接口框架简化结构图（线程安全）：</p><img src="/javabook-v2/images/map-2.png"></li></ul><p>JDK 中的集合框架首次出现在 1.0 版本，随后在 1.2 和 1.5 版本中有较大更新。其它版本的更新对于集合框架的更新都是小更新。</p><p>大体上，这三个版本的主要 feature 在于：</p><ol><li>Plan A：线性安全实现。</li><li>Plan B：线性不安全实现。性能更好。</li><li>Plan A plus：线性安全实现的另一种方案。用来和 Plan A “打擂台”。</li></ol><h2 id="_2-list" tabindex="-1"><a class="header-anchor" href="#_2-list"><span>2. List</span></a></h2><p>List 体系在 JDK 1.0 的时候就已经存在了。作为它的兄弟，Set 接口反而是在 JDK 1.2 中才被补充进集合框架。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Collection                   (jdk 1.0)</span>
<span class="line">└── List                     (jdk 1.0)</span>
<span class="line">    |── Vector / Stack       (jdk 1.0)</span>
<span class="line">    |── ArrayList            (jdk 1.2)</span>
<span class="line">    |── LinkedList           (jdk 1.2)</span>
<span class="line">    └── CopyOnWriteArrayList (jdk 1.5)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_2-1-vector" tabindex="-1"><a class="header-anchor" href="#_2-1-vector"><span>2.1 Vector</span></a></h3><p>Vector 实现的是数据结构领域中的『基于数组实现的线性表』。线性表也叫『向量』，所以它用上了 vector 这个单词。</p><p>Vector 诞生于 JDK 1.0 时代，实现的是 List 接口。底层是通过『<strong>动态数组</strong>』实现的。『动态数组』是指当数组容量不足以存放新的元素时，会创建新的数组，然后把原数组中的内容复制到新数组。</p><p><strong>特征</strong>：</p><ol><li>查询效率高、插入、删除效率低，因为需要移动元素；</li><li>线程安全。</li><li>允许添加 null 值，且允许添加多个；</li><li>未指定初始化大小时，默认的大小为 10 。每次扩容时变为原来大小的 2 倍。在批量添加时，如果扩容后仍不够，则按实际需要扩容。</li></ol><h3 id="_2-2-stack" tabindex="-1"><a class="header-anchor" href="#_2-2-stack"><span>2.2 Stack</span></a></h3><p>Stack 实现的是数据结构中的『基于数组实现的栈』。</p><p>Stack 诞生于 1.0 时代，它继承自 Vector ，间接实现了 List 接口，但它的核心逻辑还是『栈』。</p><p><strong>注意事项</strong>：</p><ul><li>『非线性安全的栈』在 Java 集合框架中都是通过使用『<strong>双端队列</strong>』来实现功能。</li></ul><h3 id="_2-3-arraylist" tabindex="-1"><a class="header-anchor" href="#_2-3-arraylist"><span>2.3 ArrayList</span></a></h3><p>ArrayList 实现的是数据结构领域中的『基于数组实现的线性表』。这和 Vector 是一样的。</p><p>ArrayList 诞生于 JDK 1.2 ，实现的是 List 接口。它的底层是通过『<strong>动态数组</strong>』实现的。</p><p><strong>特征</strong>：</p><ol><li>查询效率高、插入、删除效率低，因为需要移动元素；</li><li>线程不安全。</li><li>允许添加 null 值，且允许添加多个；</li><li>未指定初始大小时，默认大小为 0 。 扩容时容量变为原大小的 1.5 倍。批量添加时，如果扩容后仍不够，则按实际需要扩容。</li></ol><h3 id="_2-4-linkedlist" tabindex="-1"><a class="header-anchor" href="#_2-4-linkedlist"><span>2.4 LinkedList</span></a></h3><p>LinkedList 实现的是数据结构领域中的『基于链表实现的线性表』。</p><p>LinkedList 和 ArrayList 是 “双胞胎” ，都是诞生于 JDK 1.2，实现类 List 接口。LinkedList 底层使用『<strong>双向链表</strong>』实现。</p><p><strong>特性</strong>：</p><ol><li>插入和删除效率高，查询效率低；顺序访问会非常高效，而随机访问效率（比如 get 方法）比较低；</li><li>线程不安全；</li><li>允许添加 null 值，且允许添加多个；</li><li>LinkedList 不存在扩容问题。</li><li>LinkedList 除了实现 List 接口，它还『多』实现了 Deque 接口。</li></ol><h3 id="_2-5-copyonwritearraylist" tabindex="-1"><a class="header-anchor" href="#_2-5-copyonwritearraylist"><span>2.5 CopyOnWriteArrayList</span></a></h3><p>CopyOnWriteArrayList 实现的是数据结构领域中的『基于数组实现的线性表』。这和 Vector、ArrayList 是一样的。</p><p><strong>CopyOnWriteArrayList</strong> 是诞生于 JDK 1.5 ，实现了List 接口。底层通过『<strong>动态数组</strong>』实现，并且提供了不同于 Vector 的线程安全实现：</p><ul><li>写操作（add、set、remove 等等）上锁，并且把原数组拷贝一份出来，然后在新数组进行写操作，操作完后，再将原数组引用指向到新数组。</li><li>读操作不加锁，读的时原数组，因此可能读到旧的数据。比如正在执行读操作时，同时有写操作在进行，遇到这种场景时，就会都到旧数据。</li></ul><p><strong>特性</strong>：</p><ol><li>线程安全；</li><li>查询效率高；写操作，需要拷贝数组，比较消耗内存和时间；</li><li>允许添加 null 值，且允许添加多个；</li><li>不能用于实时读的场景，因为读取到数据可能是旧的，可以保证最终一致性（不保证实时一致性）。</li></ol><p>补充</p><p>JDK 1.5 还提供了 <code>Collections.synchronizedList(List list)</code> 来获得线程安全版的 List 。</p><h3 id="_2-6-arraylist、linkedlist、vector-三者的区别" tabindex="-1"><a class="header-anchor" href="#_2-6-arraylist、linkedlist、vector-三者的区别"><span>2.6 ArrayList、LinkedList、Vector 三者的区别</span></a></h3><p>核心区别：</p><table><thead><tr><th style="text-align:left;">List 的实现</th><th style="text-align:left;">线程安全</th><th style="text-align:left;">底层实现</th><th style="text-align:left;">默认初始容量 扩容系数</th></tr></thead><tbody><tr><td style="text-align:left;">Vector（JDK 1.0）</td><td style="text-align:left;">Yes</td><td style="text-align:left;">动态数组</td><td style="text-align:left;">x 2</td></tr><tr><td style="text-align:left;">ArrayList（JDK 1.2）</td><td style="text-align:left;">No</td><td style="text-align:left;">动态数组</td><td style="text-align:left;">x 1.5</td></tr><tr><td style="text-align:left;">LinkedList（JDK 1.2）</td><td style="text-align:left;">No</td><td style="text-align:left;">双向链表</td><td style="text-align:left;">无</td></tr></tbody></table><p>其它区别：</p><h2 id="_3-map" tabindex="-1"><a class="header-anchor" href="#_3-map"><span>3. Map</span></a></h2><p>Map 体系在 JDK 1.0 的时候就已经存在了，在当时是集合框架的 2 条腿之一（另一条腿是 List 体系。这个时候还没有 Set 体系，Set 体系 1.2 才出现）。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Map                    (jdk 1.0)</span>
<span class="line">|── Hashtable          (jdk 1.0)</span>
<span class="line">|── TreeMap            (jdk 1.2)</span>
<span class="line">|── HashMap            (jdk 1.2)</span>
<span class="line">|   └── LinkedHashMap  (jdk 1.4)</span>
<span class="line">└── ConcurrentHashMap  (jdk 1.5)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Map 结构是以 key-value 键值对形式存储数据。在数据结构领域，这种结构也被称作『<strong>字典</strong>（Dictionary）』。</p><p>另外，在数据结构领域，哈希表和哈希集是同一种结构的 2 种不同称呼，所以，在 JDK 里面 HashTable 和 HashMap 这两个单词都被用上了。</p><h3 id="hashtable" tabindex="-1"><a class="header-anchor" href="#hashtable"><span>HashTable</span></a></h3><p>Hashtable 对应的是数据结构中的『散列表』结构。从直译角度来看，Hashtable 这个单词要比 Hashmap 更地道。</p><p>Hashtable 诞生于 JDK 1.0 ，实现了 Map 接口。底层使用『<strong>数组+链表</strong>』实现。</p><p>补充</p><p>Hashtable 这种解决 hash 冲突的方式被称为『<strong>链地址法</strong>』，也叫拉链法。</p><p><strong>特性</strong>：</p><ol><li>线程安全。</li><li>查询、插入、删除效率都高（集成了多方特性）</li><li>key 和 value 都不允许为 null。</li><li>Hashtable 默认的初始大小为 11 ，之后每次扩充为原来的 2 倍；</li><li>除了实现 Map 接口，Hashtable 还继承了 Dictionary 类。这是它的兄弟姐妹们所没有的特征。</li></ol><h3 id="hashmap" tabindex="-1"><a class="header-anchor" href="#hashmap"><span>HashMap</span></a></h3><p>Hashmap 对应的是数据结构中的『散列表』结构。</p><p>从计算机词汇的翻译上看，hashtable 和 hashmap 是一回事（当然底层实现原理不同）。</p><p>HashMap 是诞生于 JDK 1.2，实现了 Map 接口。以前，Hashmap 和 Hashtable 的底层数据结构是一样的，都是『数组+链表』。但是在 JDK 8 中 Hashmap 的底层实现有升级：『数组+链表』or『数组+红黑树』：</p><ul><li><p>数据量不大的时候，底层使用『数组+链表』；</p></li><li><p>当数据量增加到一定程度，就变为 数组+红黑树』。链表长度 <code>&gt;=8</code> 时，并且同时数组的长度 <code>&gt;=64</code>。</p><img src="/javabook-v2/images/HashMap.png"></li><li><p>特性</p><ol><li><p>线程不安全。</p></li><li><p>查询、插入、删除效率都高（集成了多方特性）</p></li><li><p>允许 null 的键值对；</p></li><li><p>初始容量是 16，每次扩容都是变为原来大小的 2 倍；</p></li><li><p>用到了一个编程小技巧：用位运算（&amp;）来代替取模运算（%）</p><p>当 b 的值为 2^n 时，<code>a % b</code> 和 <code>a &amp; (b-1)</code> 的值相等。这样效率更高，并且可以解决负数问题（负数模运算取余，余数应该始终为正数）。</p></li></ol></li></ul><h3 id="concurrenthashmap" tabindex="-1"><a class="header-anchor" href="#concurrenthashmap"><span>ConcurrentHashMap</span></a></h3><p>ConcurrentHashMap 对应的是数据结构中的『散列表』。</p><p>ConcurrentHashMap 诞生于 JDK 1.5 时代。它实现了 Map 接口。ConcurrentHashMap 和 HashMap 一样，底层使用『<strong>数组 + 链表 + 红黑树</strong>』实现。</p><p>另外，在 Java 8 版本中，ConcurrentHashMap 有比较大的变动（更新升级），采用 CAS + Synchronized 实现锁操作，因此，性能上要好于 JDK 1.0 时代的 Hashtable 。</p><p><strong>特性</strong>：</p><ol><li>线程安全。</li><li>查询、插入、删除效率都高（集成了多方特性）</li></ol><h3 id="hashmap、hashtable、conccurenthashmap-三者的区别" tabindex="-1"><a class="header-anchor" href="#hashmap、hashtable、conccurenthashmap-三者的区别"><span>HashMap、Hashtable、ConccurentHashMap 三者的区别</span></a></h3><table><thead><tr><th style="text-align:left;">List 的实现</th><th style="text-align:left;">历史</th><th style="text-align:left;">线程安全</th><th style="text-align:left;">底层数据结构</th></tr></thead><tbody><tr><td style="text-align:left;">Hashtable</td><td style="text-align:left;">JDK 1.0</td><td style="text-align:left;">Yes（Synchronized 实现）</td><td style="text-align:left;">数组 + 链表</td></tr><tr><td style="text-align:left;">HashMap</td><td style="text-align:left;">JDK 1.2</td><td style="text-align:left;">No</td><td style="text-align:left;">数组 + 链表 + 红黑树</td></tr><tr><td style="text-align:left;">ConccurentHashMap</td><td style="text-align:left;">JDK 1.5</td><td style="text-align:left;">Yes（CAS + Synchronized 实现）</td><td style="text-align:left;">数组 + 链表 + 红黑树</td></tr></tbody></table><p>其它小细节：</p><table><thead><tr><th style="text-align:left;">List 的实现</th><th style="text-align:left;">null-key</th><th style="text-align:left;">null-value</th></tr></thead><tbody><tr><td style="text-align:left;">Hashtable</td><td style="text-align:left;">不允许</td><td style="text-align:left;">不允许</td></tr><tr><td style="text-align:left;">HashMap</td><td style="text-align:left;">允许</td><td style="text-align:left;">允许</td></tr><tr><td style="text-align:left;">ConccurentHashMap</td><td style="text-align:left;">不允许</td><td style="text-align:left;">不允许</td></tr></tbody></table><h3 id="treemap" tabindex="-1"><a class="header-anchor" href="#treemap"><span>TreeMap</span></a></h3><p>TreeMap 对应的是数据结构中的『散列表』。</p><p>TreeMap 和 HashMap 一样，也是诞生于 JDK 1.2，实现的是 Map 接口的实现类，底层使用『红黑树』实现。</p><p>另外，TreeMap 除了实现了 Map 接口，它还实现了 SotredMap 接口，意味着可以排序，是一个有序的集合，你添加进 TreeMap 中的数据会被它自动排序。</p><p><strong>特性</strong>：</p><ul><li>线程不安全；</li><li>查询、插入、删除效率都比较高。</li></ul><h3 id="linkedhashmap" tabindex="-1"><a class="header-anchor" href="#linkedhashmap"><span>LinkedHashMap</span></a></h3><ul><li>线程不安全</li><li>底层使用『数组 + 链表 + 红黑树』实现</li></ul><p>LinkedHashMap 并不是在关键节点添加进集合框架的，它是在 JDK 1.4 的时候添加进集合框架的。看起来像个补丁。</p><p>LinkedHashMap 继承自 HashMap ，它在 HashMap 的基础上用链表额外去『串』起了所有的节点，这样就能记录下添加到 LinkeHashMap 中的数据的添加顺序，从而保证了迭代顺序和添加顺序的一致。</p><h3 id="hashmap-与-treemap-的区别" tabindex="-1"><a class="header-anchor" href="#hashmap-与-treemap-的区别"><span>HashMap 与 TreeMap 的区别</span></a></h3><p>关键性区别：</p><table><thead><tr><th style="text-align:left;">Map 的实现</th><th style="text-align:left;">线程安全</th><th style="text-align:left;">底层实现</th></tr></thead><tbody><tr><td style="text-align:left;">HashMap （JDK 1.2）</td><td style="text-align:left;">No</td><td style="text-align:left;">数组 + 链表 + 红黑树，无序</td></tr><tr><td style="text-align:left;">TreeMap （JDK 1.2）</td><td style="text-align:left;">No</td><td style="text-align:left;">红黑树，有序</td></tr></tbody></table><p>其它小区别：</p><table><thead><tr><th style="text-align:left;">Map 的实现</th><th style="text-align:left;">null-key</th><th style="text-align:left;">null-val</th><th style="text-align:left;">默认初始容量</th><th style="text-align:left;">扩容系数</th></tr></thead><tbody><tr><td style="text-align:left;">HashMap （JDK 1.2）</td><td style="text-align:left;">允许</td><td style="text-align:left;">允许</td><td style="text-align:left;">16</td><td style="text-align:left;">x2</td></tr><tr><td style="text-align:left;">TreeMap （JDK 1.2）</td><td style="text-align:left;">不允许</td><td style="text-align:left;">不允许</td><td style="text-align:left;">无</td><td style="text-align:left;">无</td></tr></tbody></table><p>另外，相较而言，Hashmap 效率略高，Treemap 效率略低。</p><p>其实从使用思路上看，它们两个侧重点完全不同：HashMap 的侧重点是 <code>hash</code> ，虽然在 1.8 中用到了红黑树，但是本质上是对 1.8 以前的链表方案的升级而已（实际上，并非非用树不可）；而 TreeMap 的侧重点就是 <code>tree</code>，可以想象，即便底层不是使用的红黑树，其作者也会使用其它树结构（例如 AVL 树）。</p><h2 id="_4-set" tabindex="-1"><a class="header-anchor" href="#_4-set"><span>4. Set</span></a></h2><p>整个 Set 体系是在第二波（JDK 1.2）才被添加进集合框架中的，看起来，它好似 List 的兄弟，但是实际上，它出现地要比 List（和 Map）晚。</p><p>由于在集合框架的第一波（JDK 1.0）中并没有 Set 体系，因此 Set 体系在线程安全的实现上，就没有想 List 和 Map 一样『走弯路』：</p><ul><li>JDK 1.2 中引入是 Set 的线程不安全实现；</li><li>在 JDK 1.5（J.U.C）中引入的是 Set 的线程安全实现。</li></ul><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Set                     (jdk 1.2)</span>
<span class="line">|── TreeSet             (jdk 1.2)</span>
<span class="line">|── HashSet             (jdk 1.2)</span>
<span class="line">|   └── LinkedHashSet   (jdk 1.4)</span>
<span class="line">└── CopyOnWriteArraySet (jdk 1.5)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>一个既有意思又重要的现象：虽然 Set 看起来和 List 更亲（都是 Collection 接口的子接口），但是 Set 的各种实现类底层却是『<strong>利用 Map</strong>』实现的自己的功能。</p><h3 id="hashset" tabindex="-1"><a class="header-anchor" href="#hashset"><span>HashSet</span></a></h3><ul><li>线程不安全</li><li>底层用 HashMap 实现。根本结构是数组+链表+红黑树。</li></ul><p>HashSet 是用来存储没有重复元素的集合类，并且是无序的。或者说，你可以添加重复元素，但是从第二次添加开始，你加了也白加。</p><p>由于 HashSet 底层利用的是 HashMap 实现，因此你添加到 HashSet 中的值，都被它添加到一个 HashMap 中作了 key：</p><ul><li>由于 HashMap 的 key 不能重复，所以 HashSet 中的值自然也就是不能重复的；</li><li>由于 HashMap 允许 null-key，所以 HashSet 中也可以添加一个 null 。</li></ul><p><strong>使用场景</strong>：去重、不要求顺序</p><h3 id="treeset" tabindex="-1"><a class="header-anchor" href="#treeset"><span>TreeSet</span></a></h3><ul><li>线程不安全</li><li>底层使用 TreeMap 实现，因此根本结构是『红黑树』。</li></ul><p>TreeSet 除了实现 Set 接口，它还实现了 SortedSet 接口，意味着可以排序，它是一个有序并且没有重复的集合类。TreeMap 是有序的，因此 TreeSet 自然也就是有序的。</p><p>由于 TreeSet 底层利用的是 TreeMap 实现，因此你添加到 TreeSet 中的值，都被它添加到一个 TreeMap 中作了 key：</p><ul><li>TreeMap 是内部有序的，TreeSet 自然也就是内部有序的；</li><li>TreeMap 不允许添加 null-key，TreeSet 自然也就不允许添加 null 值。</li></ul><p>TreeSet 支持两种排序方式：自然排序（默认）和自定义排序。</p><p><strong>使用场景</strong>：去重、要求排序</p><h3 id="linkedhashset" tabindex="-1"><a class="header-anchor" href="#linkedhashset"><span>LinkedHashSet</span></a></h3><ul><li>线程不安全</li><li>HashSet 的子类，底层利用了 LinkedHashMap，因此根本结构是『数组+链表+红黑树』。</li></ul><p>由于 LinkedHashSet 底层利用了 LinkedHashMap，因此：</p><ul><li>LinkedHashMap 是 JDK 1.4 中出现的；LinkedHashSet 自然也是 JDK 1.4 才出现的；</li><li>LinkedHashMap 能记录元素的添加次序，LinkedHashSet 自然也就能记录元素的添加次序。</li></ul><p><strong>使用场景</strong>：去重、需要保证插入或者访问顺序</p><h3 id="hashset、treeset、linkedhashset-的区别" tabindex="-1"><a class="header-anchor" href="#hashset、treeset、linkedhashset-的区别"><span>HashSet、TreeSet、LinkedHashSet 的区别</span></a></h3><p><strong>历史发展层面</strong>：</p><table><thead><tr><th style="text-align:left;">Set 的实现</th><th style="text-align:left;">诞生</th></tr></thead><tbody><tr><td style="text-align:left;">HashSet</td><td style="text-align:left;">JDK 1.2</td></tr><tr><td style="text-align:left;">TreeSet</td><td style="text-align:left;">JDK 1.2</td></tr><tr><td style="text-align:left;">LinkedHashSet</td><td style="text-align:left;">JDK 1.4</td></tr></tbody></table><p><strong>线程安全层面</strong>：他们三个都是线程不安全的。</p><p>底层数据结构：</p><table><thead><tr><th style="text-align:left;">Set 的实现</th><th style="text-align:left;">底层实现</th></tr></thead><tbody><tr><td style="text-align:left;">HashSet</td><td style="text-align:left;">利用了 HashMap，去重</td></tr><tr><td style="text-align:left;">TreeSet</td><td style="text-align:left;">利用了 TreeMap，去重 + 内部有序</td></tr><tr><td style="text-align:left;">LinkedHashSet</td><td style="text-align:left;">利用了 LinkedHashMap，去重 + 外部有序</td></tr></tbody></table><h3 id="copyonwritearrayset" tabindex="-1"><a class="header-anchor" href="#copyonwritearrayset"><span>CopyOnWriteArraySet</span></a></h3><ul><li>线程安全</li><li>底层利用的是 CopyOnWriteArrayList</li></ul><p>CopyOnWriteArraySet 是 JDK 1.5（J.U.C）引入的 Set 接口的线程安全实现。</p><p><strong>特征</strong>：线程安全读多写少，比如缓存不能存储重复元素。</p><h2 id="_5-queue" tabindex="-1"><a class="header-anchor" href="#_5-queue"><span>5. Queue</span></a></h2><p>Queue 体系比较『新』。<strong>Queue</strong> 接口在 JDK 1.5 才引入到 JDK 集合框架体系，它的子接口 <strong>Deque</strong> 接口在 JDK 1.6 的时候才补充进 JDK 。</p><p>Deque 继承自 Queue ，是一个既可以在头部操作元素，又可以为尾部操作元素，俗称为双端队列。</p><blockquote><p>在数据结构领域，Queue 是一个先入先出（FIFO）的集合。</p></blockquote><p>在集合框架中它分为 3 大类：</p><ul><li>非阻塞队列：普通队列（Queue、Deque）</li><li>阻塞队列（BlockingQueue）</li><li>其它队列：主要是优先级队列 （PriorityQueue、PriorityBlockingQueue）</li></ul><h3 id="非阻塞队列-普通队列" tabindex="-1"><a class="header-anchor" href="#非阻塞队列-普通队列"><span>非阻塞队列（普通队列）</span></a></h3><p>非阻塞队列（普通队列）主要指的是 Queue 接口（JDK 1.5）和 Deque 接口（JDK 1.6）的直接实现类。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Collection</span>
<span class="line">└── Queue</span>
<span class="line">    |── ConcurrentLinkedQueue       基于链表，线程安全</span>
<span class="line">    └── Deque </span>
<span class="line">        |── ArrayDeque              基于动态数组，线程不安全</span>
<span class="line">        |── LinkedList              基于链表，线程不安全</span>
<span class="line">        └── ConcurrentLinkedDeque   基于链表，线程安全</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Queue 接口定义（引入）的方法包括：</strong></p><table><thead><tr><th style="text-align:left;"></th><th style="text-align:left;">Throws exception</th><th style="text-align:left;">Returns special value</th></tr></thead><tbody><tr><td style="text-align:left;">Insert</td><td style="text-align:left;">add(e)</td><td style="text-align:left;">offer(e)</td></tr><tr><td style="text-align:left;">Remove</td><td style="text-align:left;">remove()</td><td style="text-align:left;">poll()</td></tr><tr><td style="text-align:left;">Examine</td><td style="text-align:left;">element()</td><td style="text-align:left;">peek()</td></tr></tbody></table><p><strong>Deque 接口定义（引入）的方法包括：</strong></p><table><thead><tr><th style="text-align:left;"></th><th style="text-align:left;">Throws exception</th><th style="text-align:left;">Special value</th><th style="text-align:left;">Throws exception</th><th style="text-align:left;">Special value</th></tr></thead><tbody><tr><td style="text-align:left;">Insert</td><td style="text-align:left;">addFirst(e)</td><td style="text-align:left;">offerFirst(e)</td><td style="text-align:left;">addLast(e)</td><td style="text-align:left;">offerLast(e)</td></tr><tr><td style="text-align:left;">Remove</td><td style="text-align:left;">removeFirst()</td><td style="text-align:left;">pollFirst()</td><td style="text-align:left;">removeLast()</td><td style="text-align:left;">pollLast()</td></tr><tr><td style="text-align:left;">Examine</td><td style="text-align:left;">getFirst()</td><td style="text-align:left;">peekFirst()</td><td style="text-align:left;">getLast()</td><td style="text-align:left;">peekLast()</td></tr></tbody></table><p>在引入 Deque 接口之后，JDK 很是器重它，一方面 Deque 的功能涵盖了 Queue 的功能，另一方面，数据结构中的『栈』和『队列』都可以通过 Deque 统一实现。</p><p><strong>Queue VS Deque：</strong></p><table><thead><tr><th style="text-align:left;">Queue Method</th><th style="text-align:left;">Equivalent Deque Method</th></tr></thead><tbody><tr><td style="text-align:left;">add(e)</td><td style="text-align:left;">addLast(e)</td></tr><tr><td style="text-align:left;">offer(e)</td><td style="text-align:left;">offerLast(e)</td></tr><tr><td style="text-align:left;">remove()</td><td style="text-align:left;">removeFirst()</td></tr><tr><td style="text-align:left;">poll()</td><td style="text-align:left;">pollFirst()</td></tr><tr><td style="text-align:left;">element()</td><td style="text-align:left;">getFirst()</td></tr><tr><td style="text-align:left;">peek()</td><td style="text-align:left;">peekFirst()</td></tr></tbody></table><p><strong>Stack VS Deque：</strong></p><table><thead><tr><th style="text-align:left;">Stack Method</th><th style="text-align:left;">Equivalent Deque Method</th></tr></thead><tbody><tr><td style="text-align:left;">push(e)</td><td style="text-align:left;">addFirst(e)</td></tr><tr><td style="text-align:left;">pop()</td><td style="text-align:left;">removeFirst()</td></tr><tr><td style="text-align:left;">peek()</td><td style="text-align:left;">peekFirst()</td></tr></tbody></table><h3 id="阻塞队列" tabindex="-1"><a class="header-anchor" href="#阻塞队列"><span>阻塞队列</span></a></h3><p>阻塞队列在队列功能的基础上提供了『阻塞当前线程』的能力：在空队列获取元素时、或者在已存满队列存储元素时，都会被阻塞当前线程。<strong>阻塞队列全部都是线程安全的。</strong></p><p><strong>阻塞队列体系</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Collection</span>
<span class="line">└── Queue</span>
<span class="line">    |── BlockingQueue </span>
<span class="line">    |   |── LinkedBlockingQueue     基于链表</span>
<span class="line">    |   |── ArrayBlockingQueue      基于动态数组</span>
<span class="line">    |   └── SynchronousQueue        基于 CAS 的阻塞队列。                     |</span>
<span class="line">    └── Deque</span>
<span class="line">        └── BlockingDeque</span>
<span class="line">            └── LinkedBlockingDeque 基于链表，双端队列</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>BlockingQueue 接口引入了一套新的 API，它和 Queue 的那套 API 是独立的，互不影响，不要调用错了。</p><table><thead><tr><th style="text-align:left;"></th><th style="text-align:left;">Throws exception</th><th style="text-align:left;">Special value</th><th style="text-align:left;">Blocks</th><th style="text-align:left;">Times out</th></tr></thead><tbody><tr><td style="text-align:left;">Insert</td><td style="text-align:left;">add(e)</td><td style="text-align:left;">offer(e)</td><td style="text-align:left;">put(e)</td><td style="text-align:left;">offer(e, time, unit)</td></tr><tr><td style="text-align:left;">Remove</td><td style="text-align:left;">remove()</td><td style="text-align:left;">poll()</td><td style="text-align:left;">take()</td><td style="text-align:left;">poll(time, unit)</td></tr><tr><td style="text-align:left;">Examine</td><td style="text-align:left;">element()</td><td style="text-align:left;">peek()</td><td style="text-align:left;">not applicable</td><td style="text-align:left;">not applicable</td></tr></tbody></table><h3 id="其它队列-优先级队列" tabindex="-1"><a class="header-anchor" href="#其它队列-优先级队列"><span>其它队列（优先级队列）</span></a></h3><p>PriorityQueue 是基于最小二叉堆实现的数据结构。内部以自然顺序排序。</p><p>常见的 PriorityQueue 是线程非安全的，PriorityBlockingQueue 是线程安全的。它俩底层数据结构是一样的。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Collection</span>
<span class="line">└── Queue</span>
<span class="line">    |── PriorityQueue</span>
<span class="line">    └── BlockingQueue</span>
<span class="line">        └── PriorityBlockingQueue</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_6-集合框架的线程安全问题" tabindex="-1"><a class="header-anchor" href="#_6-集合框架的线程安全问题"><span>6. 集合框架的线程安全问题</span></a></h2><blockquote><p>很多复杂/混乱的问题从历史的角度来看会非常的清晰。</p></blockquote><p>集合的线程安全问题指的是如果两个线程同时操作同一个集合对象出现 ConcurrentModificationException 异常的情况。</p><table><thead><tr><th style="text-align:left;">接口</th><th style="text-align:left;">1.0</th><th style="text-align:left;">1.1</th><th style="text-align:left;">1.2</th></tr></thead><tbody><tr><td style="text-align:left;">List</td><td style="text-align:left;">Vector</td><td style="text-align:left;"></td><td style="text-align:left;"></td></tr><tr><td style="text-align:left;">Map</td><td style="text-align:left;">Hashtable</td><td style="text-align:left;"></td><td style="text-align:left;"></td></tr><tr><td style="text-align:left;">Set</td><td style="text-align:left;">无</td><td style="text-align:left;"></td><td style="text-align:left;"></td></tr></tbody></table><h3 id="jdk-1-0-时代的集合类" tabindex="-1"><a class="header-anchor" href="#jdk-1-0-时代的集合类"><span>JDK 1.0 时代的集合类</span></a></h3><p>JDK 1.0 时代『一步到位』地将各种集合都是现成了线程安全版。所以，这个时代出现的 Vector（List 接口的实现）、Hashtable（Map 接口的实现）都是线程安全的。</p><p>这里需要注意的是 Set 接口（及其实现类）是从 1.2 版本开始才出现的，1.2 之前并没有。</p><h3 id="jdk-1-2-时代的集合类" tabindex="-1"><a class="header-anchor" href="#jdk-1-2-时代的集合类"><span>JDK 1.2 时代的集合类</span></a></h3><p>线程安全的代价就是降低了性能。『线程安全』和『高性能』没有哪个是标准答案，它们应该是 <code>Plan A</code> 和 <code>Plan B</code> 的关系，选哪个决定权应该在使用者的手里。</p><p>如果说，1.0 提供的是 Plan A ，那么 1.2 提出的就是 Plan B 。简单来说，1.0 的 List 和 Map 接口的实现类都是线程安全的；1.2 的 List 和 Map 接口的实现类都是非线程安全的。</p><h3 id="jdk-1-2-时代的集合类的其它改动" tabindex="-1"><a class="header-anchor" href="#jdk-1-2-时代的集合类的其它改动"><span>JDK 1.2 时代的集合类的其它改动</span></a></h3><p>除了提供 List 和 Map 的 <code>Plan B</code> 之外，1.2 时代还有一个比较大的升级：出现了 Set 接口（及其实现类）。</p><p>Set 体系一出现就是 <code>Plan B</code> 版本，因此，常见的实现类 HashSet 和 TreeSet 都是非线程安全的。</p><p>另外，JDK 1.2 除了提出了非线性安全版的实现外，对线性安全版的实现还做出了『补充』：JDK 1.2 中提供的集合工具类 Collections ，有如下的一批方法：</p><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java"><pre><code class="language-java"><span class="line"><span class="token class-name">Collections</span><span class="token punctuation">.</span><span class="token function">synchronizedCollection</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name">Collections</span><span class="token punctuation">.</span><span class="token function">synchronizedList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name">Collections</span><span class="token punctuation">.</span><span class="token function">synchronizedSet</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name">Collections</span><span class="token punctuation">.</span><span class="token function">synchronizedMap</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>些方法接受非线程安全的 List、Set、Map 对象，返回与之对应的线程安全版的 List、Set、Map 对象。为了避免命名带来的记忆负担，官方还很贴心地将这些线程安全版地实现类写成了内部类的形式，你只需要以面向接口编程的思想去使用它们就行，而不必在意它们的名字叫什么（当然，看源码还是能看到的）。</p><p>所以，如果你需要线性安全的 List 和 Map，除了，使用 JDK 1.0 版中的哪些实现外，还可以用上面的方式获得它们。</p><h3 id="jdk-1-2-的未尽工作" tabindex="-1"><a class="header-anchor" href="#jdk-1-2-的未尽工作"><span>JDK 1.2 的未尽工作</span></a></h3><p>JDK 1.2 在 JDK 1.0 / 1.1 的基础上完善、更新了 Collection 和 Map 体系下的接口和实现类。但是它的工作并未完全结束。体现在两个方面：</p><ol><li>截止到 1.2，集合体系主要体现在 List、Set 和 Map 的功能，但是对于数据结构领域中的其它结构，它的功能并不丰富。例如常见的队列（Queue）结构就没有。</li><li>截止到 1.2，了解决集合的线程安全问题的方案简单粗暴（提供的同步集合类，不允许同时存在多个线程操作同一个集合对象）。而有些场景下需要在多线程同时访问同一个线程对象时还要保证线程安全性（这个要求就很高了）。</li></ol><p>在 JDK 1.2 之后的 JDK 版本中陆陆续续还有对集合框架体系的内容进行修补完善，直到 JDK 1.5 中提出了 J.U.C，它里面包含了提供并发能力的集合实现类，至此 JDK 中的集合框架算是基本完善成型，再有的工作就是零零碎碎的小修补、小完善了。</p><p>不过，不像 JDK 1.2 这样对集合框架整体进行更新，后续版本中对于集合框架的修补完善不再是版本更新的核心功能，因此这些修改并非各个版本升级时的宣传重点，直到 JDK 1.5 的 J.U.C 中又作为 feature 宣传了一下。</p><h3 id="queue-和-stack" tabindex="-1"><a class="header-anchor" href="#queue-和-stack"><span>Queue 和 Stack</span></a></h3><p>同样是作为常见的 2 中特殊的线性表结构， Queue 和 Stack 的命运在 JDK 中十分不同。</p><ul><li>Stack 是类，继承自 Vector 类，是 List/Collection 接口的间接实现类，它是 JDK 1.0 出现的最早的一批成员。</li><li>Queue 是接口，直接继承自 Collection 接口，在 JDK 1.5 才出现！而它的一个子接口 Deque（双端队列）到 JKD 1.6 时才出现。</li></ul><p>Deque 的接口的常用实现类有两个：</p><ul><li>ArrayDeque，底层使用数组实现 Deque 接口要求的方法；</li><li>LinkedList，底层使用链表实现 Deque 接口要求的方法。注意，从名字上看，你很容易忽视掉它。</li></ul><p>它们都是非线程安全的。</p><p>这里有两个小问题：</p><ol><li>JDK 没有直接提供 Stack 的非线性安全版本。在它的代码注释中，官方建议使用 Deque 接口的实现类来当作 statck 使用。</li><li>Collections 工具类中并没有直接的 <code>.synchronizedDeque()</code> 方法，这就导致没有直接的便捷的方式获得线程安全版的队列和栈对象。</li></ol><p>因此对栈和队列的使用还是要比 List、Set 和 Map 要麻烦一点。不过，不排除后续的 JDK 升级中会完善改进这两个问题。</p>`,179)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};