<template><h1 id="操作-database" tabindex="-1"><a class="header-anchor" href="#操作-database" aria-hidden="true">#</a> 操作 Database</h1>
<h2 id="创建-database" tabindex="-1"><a class="header-anchor" href="#创建-database" aria-hidden="true">#</a> 创建 Database</h2>
<div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">SHOW</span> <span class="token keyword">DATABASES</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">DATABASE</span> <span class="token operator">&lt;</span>数据库名<span class="token operator">></span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><div class="custom-container warning"><p class="custom-container-title">警告</p>
<p>上面出现的 <strong><code>&lt;&gt;</code></strong> 表示此处内容由用户自定义。<strong><code>&lt;&gt;</code></strong> 本身并非 SQL 的内容的一部分。</p>
</div>
<h2 id="删除-database" tabindex="-1"><a class="header-anchor" href="#删除-database" aria-hidden="true">#</a> 删除 Database</h2>
<div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">DROP</span> <span class="token keyword">DATABASE</span> <span class="token operator">&lt;</span>数据库名<span class="token operator">></span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">DROP</span> <span class="token keyword">DATABASE</span> <span class="token keyword">IF</span> <span class="token keyword">EXISTS</span> <span class="token operator">&lt;</span>数据库名<span class="token operator">></span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><h2 id="创建-database-并指定默认字符集" tabindex="-1"><a class="header-anchor" href="#创建-database-并指定默认字符集" aria-hidden="true">#</a> 创建 Database 并指定默认字符集</h2>
<div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">DATABASE</span> <span class="token operator">&lt;</span>数据库名<span class="token operator">></span>
  <span class="token keyword">DEFAULT</span> <span class="token keyword">CHARACTER</span> <span class="token keyword">SET</span> utf8mb4 
  <span class="token keyword">COLLATE</span> utf8mb4_unicode_ci<span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>注意</strong>：因为历史原因，MySQL 中的 <strong><code>utf8</code></strong> 并非标准意义上的 utf8 字符编码，<strong><code>utf8mb4</code></strong> 才是。<code>utf8</code> 实际上是 <code>utf8mb3</code> 的一个别名。</p>
<p><em><code>utf8mb3</code></em> 存储一个文字时，最多占 3 个字节，而 <em><code>utf8mb4</code></em> 最多可占 4 个字节。所以，如果遇到特别特别生僻的中日韩文字时，<em><code>utf8mb3</code></em> 可能仍会出现乱码问题。</p>
<h2 id="数据库引擎" tabindex="-1"><a class="header-anchor" href="#数据库引擎" aria-hidden="true">#</a> 数据库引擎</h2>
<p>『数据库引擎』是数据库管理系统（DBMS）的底层组件和核心组件，DBMS 就靠它进行创建、查询、更新 和 删除数据操作。</p>
<p>MySQL 支持选用多种数据引擎，不同的数据库引擎具有不同的，各具特色的功能。MySQL 支持的数据库引擎有：</p>
<div class="language-sql ext-sql line-numbers-mode"><pre v-pre class="language-sql"><code><span class="token keyword">SHOW</span> ENGINES<span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p><strong><code>InnoDB</code></strong> 引擎是 MySQL 5.5.5 之后的默认引擎。它支持『事务』和『外键』，这是它的两个核心特性。</p>
<p><strong><code>MyISAM</code></strong> 引擎是 MySQL 5.5.5 之前的默认引擎。它支持『全文索引』，并且早期的版本比当时的 InnoDB 更快。</p>
</template>
